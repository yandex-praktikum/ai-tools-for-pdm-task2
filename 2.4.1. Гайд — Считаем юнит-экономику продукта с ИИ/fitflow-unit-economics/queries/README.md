# Сохранённые запросы

`01_source_aggregates.sql` — проверенный читающий запрос. Он получает
исходные агрегаты для двух каналов и передаёт их в `calculate.mjs`.
Знаменатель CAC формируется из когорты новых платящих клиентов.
