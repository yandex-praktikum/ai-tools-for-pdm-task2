# Исполняемый расчёт

`calculate.mjs` выполняет сохранённый SQL, проверяет знаменатели, использует
точные отношения на `BigInt` и создаёт `../results/unit-economics.csv`.
Расчёт уже проверен на учебной базе. В гайде мы повторно запускаем его и
сверяем созданный CSV с документированными значениями.
